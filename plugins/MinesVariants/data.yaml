"model":
    "huihui_ai/deepseek-r1-abliterated:14b"
"params":
    "rule_check":
        - "你!被!接!入!了!一!个!自!动!程!序!里!面!你!需!要!做!的!只!有!判!断!"
        - "你只需要检查是不是描述扫雷规则的文本即可"
        - "检查完毕后需要返回(带双引号的True/双引号的False)"
        - "例: \"True\":后续文本是一个关于扫雷规则的描述,  \"False\":后续文本不是一个关于扫雷规则的描述"
        - "接下来花括号内为为文本: "
    "check_examp":
        - "你!被!接!入!了!一!个!自!动!程!序!里!面!你!需!要!做!的!只!有!判!断!"
        - "你只需要检查下面的描述扫雷规则的文本存不存在示例即可"
        - "如果存在示例那就返回(带双引号的True/双引号的False)"
        - "例: \"True\", 后续文本是一个关于扫雷规则的描述 \"False\", 后续文本不是一个关于扫雷规则的描述"
        - "接下来花括号内为为文本: "
    "init_examp":
        - "你!被!接!入!了!一!个!自!动!程!序!里!面!你!需!要!做!的!只!有!判!断!"
        - "禁止输出任何其他的分析和判断 必须只输出格式内的内容"
        - "在输入最结尾的花括号内存在一段文本 你需要找到里面的示例信息并按照指定格式返回文本"
        - "示例线索的文本(直接将文本中的线索放进来即可), 该示例是否是正确示范(True正确示范False错误示范)]"
        - "接下来方括号内为演示信息[[[[True, False, True], [True, False, False], [False, False, False]], (1, 1), '3', True], ...]"
        - "这个被解释为: 线索3在索引为(1,1)的位置且这个是一个合法示例 注意用户给出的示例不一定为3*3的内容"
        - "如果说不存在示例内容请输出单独一个方括号表示不存在示例内容 相对的用户可能会输出多个示例内容 那么你也需要输出多个数据"
        - "所有的输出必须在最外层包上双引号 相对的内层如果有字符串需要包上单引号 请有且仅有最外层的两个双引号不要有其他任何双引号"
        - "接下来为你需要判断的文本 文本:"
    "init_code":
        - "你!被!接!入!了!一!个!自!动!程!序!里!面!"
        - "你需要分析一个扫雷的代码并分析和按要求输出代码"
        - "以下为可以需要实现的接口:"
        - "需要实现一个继承了(.rule.ClueValue)的类用来存储线索信息"
        - "在该类中必须实现__init__(self,code:str)函数来初始化所需要的数据,code为code函数的返回值"
        - "在该类中必须实现__repr__(self)->str函数来显示输出和显示该线索"
        - "在该类中必须实现@classmethod修饰的type(cls)->str函数来返回该线索的属于哪个规则"
        - "在该类中必须实现code(self)->str来实现数据的格式化和初始化"
        - "在该类中必须实现check(self,board:Board,pos:Pos)->bool"
        - "check函数为主要核心其中board为题板输入pos为当前线索的位置信息"
        - "你需要返回当前题板对于这个线索是否严格非法:True合法或未知,False严格非法"
        - "还需要实现一个继承了(.rule.ClueRule)的类来表示为该规则"
        - "在该规则类中选择实现__init__(self)函数来初始化该规则"
        - "在该规则类中必须实现fill(self,board:Board,random:Random)"
        - "该方法用来填充题板中所有None为规则线索实例对象 参数board为题板对象,random为python内置random对象"
        - "接下来为Board对象的接口"
        - "在该题板类中实现了__call__(target:str)->generator[tuple[ClueValue, Pos]]"
        - "target为你希望的遍历类型target:遍历目标类型 C:线索 F:雷 N:未定义(None)"
        - "通常使用\'for value, pos in board(\"CF\"):\'来表示遍历所有的线索格和雷格"
        - "在该题板类中实现了clone()->Board 表示克隆一个题板且操作不影响源题板"
        - "在该题板类中实现了type_pos(pos: Pos)->str "
        - "该函数表示检查题板该位置的数据属于什么类型 C:线索 F:雷 N:未定义(None)|超出题板范围"
        - "在该题板类中实现了type_value(value)->str "
        - "该函数表示检查该数据属于什么类型 C:线索 F:雷 N:未定义(None)|超出题板范围"
        - "在该题板类中实现了get_pos(pos)->ClueValue | MinesValue | None"
        - "该函数表示返回题板中的该位置的实体对象"
        - "在该题板类中实现了set_value(pos, value) -> None"
        - "该函数表示设置题板中指定位置为指定实体对象"
        - "在该题板类中实现了clear_board()->None 表示清空题板"
        - "在该题板类中实现了dyeing_pos(pos)->bool 表示该位置是否被染色了"
        - "返回需要在文件头添加三个#(井号)来表示为文件头 且文件末尾也需要添加三个#(井号)来表示为文件尾"
        - "接下来为你需要实现的要求:"